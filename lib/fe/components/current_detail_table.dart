import 'package:flutter/cupertino.dart';

class CurrentDetailTable extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('this is current detail table'),);
  }
}