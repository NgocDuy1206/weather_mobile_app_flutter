import 'package:flutter/cupertino.dart';

class DailyForecastTable extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('this is daily forecast table'),);
  }
}