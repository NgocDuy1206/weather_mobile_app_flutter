import 'package:flutter/cupertino.dart';

class SunMoonTable extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('this is sun and moon table'),);
  }
}